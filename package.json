{
  "name": "websocket-game-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../infra && npm install && cd ../frontend && npm install",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "deploy": "bash scripts/deploy.sh",
    "destroy": "cd infra && npx cdk destroy --profile sandbox"
  }
}
